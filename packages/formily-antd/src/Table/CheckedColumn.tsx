import React, { Fragment } from 'react';
type CheckedColumnProps = {
    dataIndex: string;
};

const CheckedColumn: React.FC<CheckedColumnProps> = (props) => {
    return <Fragment></Fragment>;
};

export default CheckedColumn;

export { CheckedColumnProps };
