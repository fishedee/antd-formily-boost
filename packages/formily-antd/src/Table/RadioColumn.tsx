import React, { Fragment } from 'react';
type RadioColumnProps = {
    dataIndex: string;
};

const RadioColumn: React.FC<RadioColumnProps> = (props) => {
    return <Fragment></Fragment>;
};

export default RadioColumn;

export { RadioColumnProps };
